{% extends 'base.html.twig' %}

{% block title %}Historique des Paiements{% endblock %}

{% block body %}
{# <div class="d-flex">
    <!-- Sidebar -->
    <div class="sidebar bg-dark text-white p-3 vh-100">
        <h4 class="mb-4"><i class="bi bi-gem text-warning"></i> Dashboard</h4>
        {# <ul class="nav flex-column">
            <li class="nav-item mb-2">
                <a href="{{ path('app_payment_historique') }}" class="nav-link text-white active">
                    <i class="bi bi-clock-history me-2"></i>Historique
                </a>
            </li>
            <li class="nav-item mb-2">
                <a href="#" class="nav-link text-white">
                    <i class="bi bi-bar-chart-line me-2"></i>Statistiques
                </a>
            </li>
            <li class="nav-item mb-2">
                <a href="#" class="nav-link text-white">
                    <i class="bi bi-person-circle me-2"></i>Profil
                </a>
            </li>
        </ul> #}
        {# <ul class="nav nav-pills flex-column mb-auto">
            <li class="nav-item mb-2">
                <a href="{{ path('app_dashboard') }}" class="nav-link active text-white">
                    <i class="bi bi-house-door me-2"></i>Vue d'ensemble
                </a>
            </li>
            
            <li class="nav-item mb-2">
                <a href="{{ path('event_my_events') }}" class="nav-link text-white">
                    <i class="bi bi-calendar-event me-2"></i>Mes événements
                </a>
            </li>
            <li class="nav-item mb-2">
                <a href="{{ path('analytics_index') }}" class="nav-link text-white">
                    <i class="bi bi-graph-up me-2"></i>Statistiques
                </a>
            </li>
            <li class="nav-item mb-2">
                <a href="{{ path('app_payment_historique')}}" class="nav-link text-white">
                    <i class="bi bi-house-door me-2"></i>Historique
                </a>
            </li>
            <li class="nav-item mb-2">
                <a href="{{ path('scanner_index') }}" class="nav-link text-white">
                    <i class="bi bi-qr-code-scan me-2"></i>Scanner QR
                </a>
            </li>

            {# <li class="nav-item mb-2">
                <a href="{{ path('payment_history') }}" class="nav-link text-white">
                    <i class="bi bi-credit-card me-2"></i>Paiements
                </a>
            </li> #}
            
        {# </ul>
    </div>   #}
<div class="bg-light min-vh-100">
    <div class="container-fluid">
        <div class="row">
            <!-- Sidebar -->
            <div class="col-lg-2 col-md-3 px-0 bg-dark text-white" style="min-height: calc(100vh - 56px);">
                <div class="d-flex flex-column p-3 sticky-top" style="height: calc(100vh - 56px); overflow-y: auto;">
                    <h5 class="text-uppercase fw-bold mb-4 border-bottom pb-3">
                        <i class="bi bi-speedometer2 me-2"></i>Dashboard
                    </h5>
                    
                    <ul class="nav nav-pills flex-column mb-auto">
                        <li class="nav-item mb-2">
                            <a href="{{ path('app_dashboard') }}" class="nav-link active text-white">
                                <i class="bi bi-house-door me-2"></i>Vue d'ensemble
                            </a>
                        </li>
                        
                        <li class="nav-item mb-2">
                            <a href="{{ path('event_my_events') }}" class="nav-link text-white">
                                <i class="bi bi-calendar-event me-2"></i>Mes événements
                            </a>
                        </li>
                        <li class="nav-item mb-2">
                            <a href="{{ path('analytics_index') }}" class="nav-link text-white">
                                <i class="bi bi-graph-up me-2"></i>Statistiques
                            </a>
                        </li>
                        <li class="nav-item mb-2">
                            <a href="{{ path('app_payment_historique')}}" class="nav-link text-white">
                                <i class="bi bi-house-door me-2"></i>Historique
                            </a>
                        </li>
                        <li class="nav-item mb-2">
                            <a href="{{ path('scanner_index') }}" class="nav-link text-white">
                                <i class="bi bi-qr-code-scan me-2"></i>Scanner QR
                            </a>
                        </li>

                        {# <li class="nav-item mb-2">
                            <a href="{{ path('payment_history') }}" class="nav-link text-white">
                                <i class="bi bi-credit-card me-2"></i>Paiements
                            </a>
                        </li> #}
                        
                    </ul>
                    
                    <hr>
                    
                    <div class="d-grid gap-2">
                        <a href="{{ path('event_new') }}" class="btn btn-success">
                            <i class="bi bi-plus-circle me-2"></i>Nouvel événement
                        </a>
                    </div>
                </div>
            </div>

            <!-- Main Content -->
            <div class="col-lg-10 col-md-9 py-4">
                <!-- Header -->
                <div class="d-flex justify-content-between align-items-center mb-4">
                    <div>
                        <h1 class="h2 fw-bold">Tableau de bord</h1>
                        <p class="text-muted">Bienvenue, {{ app.user.name }}! Voici un aperçu de vos événements.</p>
                    </div>
                    <div class="d-flex gap-2">
                        <button class="btn btn-outline-secondary">
                            <i class="bi bi-download me-2"></i>Exporter
                        </button>
                        <div class="dropdown">
                            <button class="btn btn-outline-primary dropdown-toggle" type="button" data-bs-toggle="dropdown">
                                <i class="bi bi-calendar3 me-2"></i>Période
                            </button>
                            <ul class="dropdown-menu">
                                <li><a class="dropdown-item" href="#">Aujourd'hui</a></li>
                                <li><a class="dropdown-item" href="#">Cette semaine</a></li>
                                <li><a class="dropdown-item" href="#">Ce mois</a></li>
                                <li><a class="dropdown-item" href="#">Cette année</a></li>
                            </ul>
                        </div>
                    </div>
                </div>

             <!-- Stats Cards -->
                <div class="row g-4 mb-4">
                    <div class="col-xl-3 col-md-6">
                        <div class="card border-0 shadow-sm h-100 dashboard-stat-card">
                            <div class="card-body">
                                <div class="d-flex align-items-center justify-content-between">
                                    <div>
                                        <p class="text-muted mb-1 fw-medium">Revenus totaux</p>
                                        <h3 class="fw-bold text-dark mb-2">{{ (totalRevenue / 100)|number_format(0, ',', ' ') }} FCFA</h3>
                                        <small class="text-success">
                                            <i class="bi bi-arrow-up me-1"></i>+12.5% ce mois
                                        </small>
                                    </div>
                                    <div class="stat-icon bg-success bg-opacity-10">
                                        <i class="bi bi-currency-dollar text-success"></i>
                                    </div>
                                </div>
                            </div>
                        </div>
                    </div>

                    <div class="col-xl-3 col-md-6">
                        <div class="card border-0 shadow-sm h-100 dashboard-stat-card">
                            <div class="card-body">
                                <div class="d-flex align-items-center justify-content-between">
                                    <div>
                                        <p class="text-muted mb-1 fw-medium">Billets vendus</p>
                                        <h3 class="fw-bold text-dark mb-2">{{ totalTicketsSold|number_format(0, ',', ' ') }}</h3>
                                        <small class="text-success">
                                            <i class="bi bi-arrow-up me-1"></i>+8.2% ce mois
                                        </small>
                                    </div>
                                    <div class="stat-icon bg-primary bg-opacity-10">
                                        <i class="bi bi-ticket-perforated text-primary"></i>
                                    </div>
                                </div>
                            </div>
                        </div>
                    </div>

                    <div class="col-xl-3 col-md-6">
                        <div class="card border-0 shadow-sm h-100 dashboard-stat-card">
                            <div class="card-body">
                                <div class="d-flex align-items-center justify-content-between">
                                    <div>
                                        <p class="text-muted mb-1 fw-medium">Événements actifs</p>
                                        <h3 class="fw-bold text-dark mb-2">{{ totalEvents }}</h3>
                                        <small class="text-success">
                                            <i class="bi bi-arrow-up me-1"></i>+25% ce mois
                                        </small>
                                    </div>
                                    <div class="stat-icon bg-warning bg-opacity-10">
                                        <i class="bi bi-calendar-event text-warning"></i>
                                    </div>
                                </div>
                            </div>
                        </div>
                    </div>

                    <div class="col-xl-3 col-md-6">
                        <div class="card border-0 shadow-sm h-100 dashboard-stat-card">
                            <div class="card-body">
                                <div class="d-flex align-items-center justify-content-between">
                                    <div>
                                        <p class="text-muted mb-1 fw-medium">Taux de conversion</p>
                                        <h3 class="fw-bold text-dark mb-2">68.5%</h3>
                                        <small class="text-success">
                                            <i class="bi bi-arrow-up me-1"></i>+3.1% ce mois
                                        </small>
                                    </div>
                                    <div class="stat-icon bg-info bg-opacity-10">
                                        <i class="bi bi-graph-up-arrow text-info"></i>
                                    </div>
                                </div>
                            </div>
                        </div>
                    </div>
                </div>
    <!-- Contenu principal -->
    {# <div class="content flex-grow-1 p-4"> #}
        <h1 class="mb-4 fw-bold text-center">
            <i class="bi bi-gem text-warning"></i> Historique
        </h1>

        <!-- Statistiques globales -->
        <div class="row mb-4 text-center">
            <div class="col-md-4">
                <div class="card shadow-sm border-0 rounded-4 p-3">
                    <h5>Total Paiements</h5>
                    <p class="fs-4 fw-bold text-primary">{{ payments|length }}</p>
                </div>
            </div>
            <div class="col-md-4">
                <div class="card shadow-sm border-0 rounded-4 p-3">
                    <h5>Montant Total</h5>
                    <p class="fs-4 fw-bold text-success">
                        {{ payments|map(p => p.amount)|reduce((carry, v) => carry + v, 0) }} XOF
                    </p>
                </div>
            </div>
            <div class="col-md-4">
                <div class="card shadow-sm border-0 rounded-4 p-3">
                    <h5>Dernier Paiement</h5>
                    <p class="fs-5 fw-semibold">
                        {% if payments|length > 0 %}
                            {{ payments|first.createdAt|date('d/m/Y H:i') }}
                        {% else %}
                            Aucun
                        {% endif %}
                    </p>
                </div>
            </div>
        </div>

        <!-- Tableau des paiements -->
        <div class="card shadow-lg border-0 rounded-4">
            <div class="card-body">
                <table class="table table-hover align-middle">
                    <thead class="table-dark">
                        <tr>
                            <th>#</th>
                            <th>Utilisateur</th>
                            <th>Montant</th>
                            <th>Méthode</th>
                            <th>Statut</th>
                            <th>Date</th>
                            <th>Facture</th>
                        </tr>
                    </thead>
                    <tbody>
                        {% for payment in payments %}
                            <tr>
                                <td>{{ payment.id }}</td>
                                <td>{{ payment.user.email }}</td>
                                <td><strong>{{ payment.getFormattedAmount() }}</strong></td>
                                <td>{{ payment.method }}</td>
                                <td>
                                    {% if payment.status == 'success' or payment.status == 'terminé' %}
                                        <span class="badge bg-success">{{ payment.status }}</span>
                                    {% elseif payment.status == 'pending' %}
                                        <span class="badge bg-warning text-dark">En attente</span>
                                    {% elseif payment.status == 'failed' %}
                                        <span class="badge bg-danger">Échoué</span>
                                    {% else %}
                                        <span class="badge bg-secondary">{{ payment.status }}</span>
                                    {% endif %}
                                </td>
                                <td>{{ payment.createdAt|date('d/m/Y H:i') }}</td>
                                <td>
                                    {% if payment.invoicePath %}
                                        <a href="{{ asset(payment.invoicePath) }}" target="_blank" class="btn btn-sm btn-outline-primary">
                                            <i class="bi bi-file-earmark-text"></i> Voir
                                        </a>
                                    {% else %}
                                        -
                                    {% endif %}
                                </td>
                            </tr>
                        {% else %}
                            <tr>
                                <td colspan="7" class="text-center text-muted">Aucun paiement trouvé</td>
                            </tr>
                        {% endfor %}
                    </tbody>
                </table>
            </div>
        </div>
    </div>
</div>

<style>
.sidebar {
    width: 250px;
    position: fixed;
    top: 0;
    left: 0;
}
.content {
    margin-left: 250px;
}
.sidebar .nav-link.active {
    background: rgba(255, 193, 7, 0.2);
    border-radius: 8px;
    font-weight: bold;
}
</style>
{% endblock %}
